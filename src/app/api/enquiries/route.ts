import {NextResponse} from "next/server";import {bookingSchema} from "@/lib/booking-schema";
export async function POST(request:Request){try{const body:unknown=await request.json();const parsed=bookingSchema.safeParse(body);if(!parsed.success)return NextResponse.json({error:"Please check the highlighted fields.",issues:parsed.error.flatten().fieldErrors},{status:400});const reference=`BRU-${Date.now().toString(36).toUpperCase()}`;/* Persistence boundary: send parsed.data to Supabase, Prisma, Resend, or your CRM here. */return NextResponse.json({ok:true,reference,message:"Your journey request has been received."},{status:201})}catch{return NextResponse.json({error:"Invalid request body."},{status:400})}}
