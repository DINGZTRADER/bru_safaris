import type { Metadata } from "next";
import { EnquiryForm } from "@/components/enquiry-form";
import { businessHours, site, socialProfiles } from "@/content/site";

export const metadata: Metadata = { title: "Contact Bru Safaris in Kigali", description: "Email, call or WhatsApp the Bru Safaris team in Kicukiro, Kigali, or request a custom East Africa itinerary.", alternates: { canonical: "/contact" } };
export default function Contact() { const whatsappUrl = `https://wa.me/${site.whatsapp.replace(/\D/g, "")}`; return <div className="page"><header className="page-hero"><p className="kicker">Get in touch</p><h1>Let’s talk<br/><em>East Africa.</em></h1><p>Send our Kigali team a note and we’ll respond personally.</p></header><section className="contact-details section"><div><p className="kicker">Contact</p><h2>We’re<br/><em>listening.</em></h2><address><a href={`mailto:${site.email}`}>{site.email}</a><a href={`tel:${site.phone}`}>{site.phone}</a><a href={whatsappUrl}>WhatsApp {site.whatsapp}</a><span>{site.address}</span></address></div><div><p className="kicker">Business hours</p><dl>{businessHours.map(({ days, hours }) => <div key={days}><dt>{days}</dt><dd>{hours}</dd></div>)}</dl><p className="kicker social-heading">Find us</p><ul className="social-labels">{socialProfiles.map(({ network, label }) => <li key={network}><b>{network}</b><span>{label}</span></li>)}</ul><p className="profile-note">Direct profile links will be added after the official URLs are verified.</p></div></section><section className="form-shell"><div><span className="form-number">01</span><h2>Plan your<br/><em>journey.</em></h2><p>Share your ideas and our team will respond personally.</p></div><EnquiryForm trip="General safari enquiry"/></section></div>; }
