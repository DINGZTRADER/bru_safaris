import type { Metadata } from "next";import Image from "next/image";import Link from "next/link";import {destinations} from "@/content/site";
export const metadata:Metadata={title:"Rwanda Destinations",description:"Discover Volcanoes, Akagera and Nyungwe national parks with a Kigali-based private safari team."};
export default function Page(){return <div className="page"><header className="page-hero"><p className="kicker">A country of many worlds</p><h1>Explore<br/><em>Rwanda.</em></h1><p>Volcanic highlands, open savannah and ancient rainforest are all within a day’s journey.</p></header><div className="destination-row">{destinations.map(d=><Link className="destination-card" href={`/destinations/${d.slug}`} key={d.slug}><Image src={d.image} fill alt={d.name}/><div><span>{d.eyebrow}</span><h3>{d.name}</h3><p>{d.text}</p></div></Link>)}</div></div>}
