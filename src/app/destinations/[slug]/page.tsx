import {destinations,tours} from "@/content/site";import {notFound} from "next/navigation";import Image from "next/image";import Link from "next/link";
export function generateStaticParams(){return destinations.map(d=>({slug:d.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=destinations.find(x=>x.slug===slug);if(!d)notFound();return <><section className="detail-hero"><Image src={d.image} fill priority alt={d.name}/><div className="hero-shade"/><div><p className="kicker light">{d.eyebrow}</p><h1>{d.name}</h1><p>{d.text}</p></div></section><div className="detail"><aside><p>Best for <b>Nature & discovery</b></p><p>Ideal stay <b>2–3 nights</b></p><Link className="button" href="/plan">Plan a visit →</Link></aside><article><p className="kicker">Know before you go</p><h2>A landscape<br/><em>with a pulse.</em></h2><p>We shape your time here around season, interests and a comfortable rhythm. Conditions, permits and park regulations can change; your planner will confirm current details before booking.</p><h3>Pair it with a journey</h3>{tours.slice(0,2).map(t=><p key={t.slug}><Link className="text-link" href={`/tours/${t.slug}`}>{t.title} →</Link></p>)}</article></div></>}
