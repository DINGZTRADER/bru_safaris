"use client";import {useState} from "react";import {Check} from "lucide-react";import {Button} from "@/components/ui/button";
export function AccommodationToggle({basePrice}:{basePrice:number}){const [style,setStyle]=useState<"mid"|"luxury">("luxury");const price=style==="luxury"?basePrice:Math.round(basePrice*.74);return <div className="rounded-3xl bg-jungle p-6 text-white"><p className="eyebrow text-gold">Choose your comfort</p><div className="mt-4 grid grid-cols-2 rounded-full bg-white/10 p-1"><button onClick={()=>setStyle("mid")} className={`rounded-full px-3 py-2 text-xs font-semibold ${style==="mid"?"bg-white text-jungle":""}`}>Mid-range</button><button onClick={()=>setStyle("luxury")} className={`rounded-full px-3 py-2 text-xs font-semibold ${style==="luxury"?"bg-gold text-jungle":""}`}>Luxury</button></div><p className="mt-7 text-sm text-white/60">Indicative price from</p><p className="display mt-1 text-4xl">${price.toLocaleString()}</p><p className="text-xs text-white/50">per person sharing · USD</p><ul className="mt-6 space-y-3 text-sm text-white/75">{["Private expert guide & 4×4","Selected permits & park fees",style==="luxury"?"Exceptional luxury lodges":"Characterful mid-range lodges","Airport transfers & 24/7 support"].map(x=><li key={x} className="flex gap-2"><Check className="size-4 text-gold"/>{x}</li>)}</ul><Button asChild variant="gold" className="mt-7 w-full"><a href={`/plan?journey=${encodeURIComponent(style)}`}>Tailor this journey</a></Button></div>}
