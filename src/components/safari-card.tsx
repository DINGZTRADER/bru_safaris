import Image from "next/image";import Link from "next/link";import {ArrowUpRight,Clock,MapPin} from "lucide-react";import type {Safari} from "@/lib/data";import {Card} from "@/components/ui/card";
export function SafariCard({safari}:{safari:Safari}){return <Card className="group overflow-hidden border-0 bg-card"><Link href={`/safaris/${safari.slug}`}><div className="relative aspect-[4/3] overflow-hidden"><Image src={safari.image} alt={`${safari.title} in ${safari.location}`} fill sizes="(max-width:768px) 100vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-black/45 to-transparent"/><p className="absolute left-5 top-5 rounded-full bg-white/90 px-3 py-1 text-[10px] font-bold uppercase tracking-[.15em] text-jungle">{safari.eyebrow}</p></div><div className="p-6"><div className="flex items-start justify-between"><h3 className="display text-2xl">{safari.title}</h3><ArrowUpRight className="size-5 text-gold transition group-hover:translate-x-1 group-hover:-translate-y-1"/></div><div className="mt-3 flex gap-4 text-xs text-muted-foreground"><span className="flex gap-1"><MapPin className="size-3.5"/>{safari.location}</span><span className="flex gap-1"><Clock className="size-3.5"/>{safari.duration}</span></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-muted-foreground">{safari.description}</p><div className="mt-5 border-t pt-4 text-sm"><span className="text-muted-foreground">From </span><strong>${safari.price.toLocaleString()}</strong><span className="text-muted-foreground"> / person</span></div></div></Link></Card>}
