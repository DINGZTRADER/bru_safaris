"use client";import {useState} from "react";import {ChevronLeft,ChevronRight,Quote,Star} from "lucide-react";import {testimonials} from "@/lib/data";import {Button} from "@/components/ui/button";
export function Testimonials(){const [i,setI]=useState(0);const item=testimonials[i];return <div className="grid items-end gap-8 md:grid-cols-[1fr_auto]"><div><div className="flex gap-1 text-gold">{[1,2,3,4,5].map(x=><Star className="size-4 fill-current" key={x}/>)}</div><Quote className="mt-8 size-10 text-gold/50"/><blockquote className="display mt-4 max-w-4xl text-3xl leading-tight md:text-5xl">“{item.quote}”</blockquote><div className="mt-8"><strong>{item.name}</strong><p className="text-sm text-muted-foreground">{item.from} · Verified on {item.source}</p></div></div><div className="flex gap-2"><Button variant="outline" size="icon" onClick={()=>setI((i-1+testimonials.length)%testimonials.length)} aria-label="Previous review"><ChevronLeft/></Button><Button variant="outline" size="icon" onClick={()=>setI((i+1)%testimonials.length)} aria-label="Next review"><ChevronRight/></Button></div></div>}
