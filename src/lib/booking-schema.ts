import {z} from "zod";
export const bookingSchema=z.object({name:z.string().min(2,"Please enter your name"),email:z.email("Enter a valid email"),phone:z.string().min(7,"Enter a valid phone number"),startDate:z.string().min(1,"Choose a travel date"),groupSize:z.union([z.number(),z.string()]).refine(value=>{const count=Number(value);return Number.isInteger(count)&&count>=1&&count<=30},"Enter 1–30 travellers"),budget:z.string().min(1,"Select a budget"),style:z.enum(["Luxury","Mid-range","A thoughtful mix"]),experiences:z.array(z.string()).min(1,"Choose at least one experience"),notes:z.string().max(1000).optional(),currency:z.enum(["USD","RWF","UGX"])});export type BookingInput=z.infer<typeof bookingSchema>;
